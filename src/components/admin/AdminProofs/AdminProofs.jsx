import { Container } from "@mui/material";

const AdminProofs = () => {
    return (
        <Container
            sx={{
                display: "flex",
                flexDirection: "column",
                justifyContent: "center"
            }}
        ></Container>
    );
};

export { AdminProofs };
